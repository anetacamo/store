import React from "react";
import "../main.scss";
import Directory from "../components/Directory";

const Homepage = () => {
  return (
    <div>
      <Directory />
    </div>
  );
};

export default Homepage;
